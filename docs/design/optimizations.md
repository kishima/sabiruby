# 高速化の記録: 何を、なぜ、どう変えて、どれだけ効いたか

2026-09-15 の一日で行った高速化を、1 か所に通して書いたもの。数値の表は
[`../verification/bench.md`](../verification/bench.md)、過程（何を疑い、何を試し、何を捨てたか）は
`../worklog/2026-09-15-stage2-perf.md` と `2026-09-15-stage2c-array-hash.md`、計画は
[`../plans/host-bridge-plan.md`](../plans/host-bridge-plan.md)。ここは説明の側で、数値は要点だけ引く。
値の表現と残っている構造的なコストの短い整理は [`performance.md`](performance.md)。

## 1. 出発点と現在地

| | 出発点（`e9da768`） | 段階 2c の後（`9da4724`） | 現在（`2aa4f13`、段階 2d の後） |
|---|---|---|---|
| 全体（本家比、合計 / 中央値） | 4.32x / 3.68x | 3.01x / 3.05x | **2.82x / 3.05x** |
| データ構造（`so_lists`、Array、Hash、String） | 10.85x | 4.57x | **3.31x**（`ds_hash` 2.85x） |
| 命令ループ | 5.71x | 3.83x | 3.10x（2d で `vmo_*` 5 本を加えた分類） |
| 呼び出し | 3.40x | 2.98x | 3.15x（同上、内訳が変わった） |
| 実アプリ寄り（fib、tak、ロボット、JSON 風、元の `vm_optimization_bench`） | 3.40x | 2.84x | 3.13x（同上） |
| メモリ（短命・長命の割り当て、GC） | 1.06x | 0.92x | 0.94x |

2026-09-16 に `printf`/`putc` が入って `bm_ao_render` と `bm_mandel_term` も動くようになり、ベンチは **27 本**になった
（`519eb17`、27 本の合計で **2.68x**、中央値 2.87x。`2aa4f13` と共通の 25 本では 2.82x → 2.75x で、これは同じ木を
別の日に測った差の範囲）。以下の表は 25 本までの記録である。

2d で `vm_optimization_bench` を 5 本に分けて分類し直したので、分類の合計は 2c までと直接は比べられない。
共通のベンチだけで比べると、出発点から現在で **−40.6%**、2c から 2d で −13.5%（`../verification/bench.md`）。

出発点の計測（段階 1、ベンチを 5 分類 22 本に分けた最初の結果）が示したのは、遅さの中心が **Hash と String**
（12.9x、11.4x）にあり、命令ループと呼び出しは 3〜4 倍で、Array は 4.6x だということだった。
つまり「値の出し入れ（`Slot`）が重い」のではなく、特定のデータ構造の実装に O(n) や複製が残っていた。
一日の作業で全体は −41%（共通のベンチ）、遅くなったベンチは 1 本も無い。

## 2. 改善の一覧

累積は出発点からの通しの計測、単独は直前のコミットとの交互 A/B（4 節）。

| # | 変えたもの | 原因 | 単独 | 主な効き先 | コミット |
|---|---|---|---:|---|---|
| 1 | opcode ごとの実行回数を**既定で数えない**、数えるときは固定長配列 | 毎命令の `Vec` 読み直しと同じ番地への store→load 依存 | **−5.6%** | 密なループで 20〜25% | `815ba7f` |
| 2 | 実行ループが `CallInfo` を**丸ごと clone しない**（3 フィールドだけ読む） | 13 フィールド 56 バイトの複製とレジスタ圧迫 | −2.8% | ほぼ全部 | `fcf0772` |
| 3 | メソッド探索が **Copy な値**（`MethodRef`）を返す | `Arc` を含む enum の `Clone` が呼び出しごとに走る | −2.1% | fib −5.2%、呼び出し | `68261b0` |
| 4 | 命令バイト → 列挙の変換を表引きから **`match`** に | 表引きの 1 ロード（`transmute` を消した代償） | −1.7% | ディスパッチ律速のループ | `5119773` |
| 5 | **メソッドキャッシュ**（世代番号で無効化） | クラスチェーンを毎回 `HashMap` で歩く | −1.3% | 呼び出し | `4a6826e` |
| 6 | `String#[]` が**全体を複製しない**、ASCII の早道 | 呼ぶたびに文字列の複製 + UTF-8 を 1 バイトずつ数える | −8.7%（7 と合わせて） | `ds_string` 7.9 倍 | `05f6f28` |
| 7 | Hash の走査で**借用を 1 回**に | 1 要素ごとにヒープを引き直す | （同上） | `ds_hash` −25〜32% | `05f6f28` |
| 8 | `Array#shift` を **O(1)**（開始オフセット） | `Vec::remove(0)` の前詰め | −6.1% | `so_lists` −73% | `35d58a5` |
| 9 | Hash の書き込みを**入口 1 本**に（性能は不変） | 索引の無効化を漏らさないため | −0.3% | — | `8ba2336` |
| 10 | Hash に **16 要素超で索引** | 挿入順の線形探索が O(n) | −7.1% | `ds_hash` −20%、`vm_optimization_bench` −14% | `e2b026b` |
| 11 | Hash の引きで**全鍵を複製しない**（`hash_sync` の順番） | 「古いか」を聞く前に全要素の鍵を `Vec` に複製 | **−11.7%** | `ds_hash` −63%、`vmo_objects` −85%、`call_kwargs` −15% | `07659aa` |
| 12 | `hash_set` で鍵のハッシュを 1 回、String 鍵の複製は挿入時だけ | 2 回計算と毎回の複製・凍結 | ±0（micro で効く） | `h[k] = v` | `2521b79` |
| 13 | `Array#include?`/`member?`/`count` が 1 要素ずつ読む | ループ内で `items()` を呼ぶ O(n²) | ±0（ベンチは呼ばない） | `count` 100 要素 −86% | `f222934` |
| 14 | `OP_GETIDX`/`GETIDX0`/`SETIDX` が Array・Hash・String に**本家と同じ早道**を持つ（残りは今のフレームで送る） | 添字 1 つの `[]` が毎回メソッド探索 + `funcall`、`Array#[]` は添字ごとに `Vec` を 1 つ確保、`String#[]` は mruby-regexp の包みを 2 段通る | ±0（全体）、`data structures` −3.7% | `vmo_index` −7.3%、`ds_hash` −8.0%、`ds_string` −3.5%、`ds_array` −4.1% | `getidx-dispatch` |

| 15 | 答えが 1 要素か数要素のネイティブが配列を**複製せず借りる**（`fetch`・`at`・`first(n)`・`last(n)`・`take`・`drop`・`values_at`・`__svalue`・引数無しの `count`・Range の `[]` ほか） | 1 要素を読むために O(n) の複製 | ±0（ベンチ） | micro で −52%、`first`/`last` 系 −59% | `e27d9a4` |
| 16 | `dup`・`initialize_copy`・`replace` が要素を**1 回だけ**写す | `Vec<Slot>` → `Vec<Value>` → `Vec<Slot>` と 2 回写していた | ±0（ベンチ） | micro で −17% | `87ad19b` |
| 17 | `assoc`・`rassoc`・`__ary_index` が**毎回配列を読み直す**（本家 `ary_assoc` と同じ） | snapshot を配っていた（`==` が配列を変えると本家と答えが違う） | ±0 | 速さのためではない | `e877d17` |

2026-09-16 の第 3 弾（`../plans/perf3-plan.md` の 3a・3b、`../worklog/2026-09-16-perf3.md`）。
単独の数値は **`codegen-units=1` で作った 2 つのバイナリ**の交互 A/B（7 ラウンド）である（4 節を見よ）。
当時は既定が 16 単位だったので、信号を見るためにわざわざ 1 単位で作っている。
2026-09-17（`96221fb`）以降は 1 単位が既定なので、この区別は無い。

| # | 変えたもの | 原因 | 単独 | 主な効き先 | コミット |
|---|---|---|---:|---|---|
| 18 | `String#<<` が**その場で伸びる** | 受け手を `Vec` に複製し、伸ばし、書き戻していた（1 回の追記が O(n)、追記のループが O(n²)） | `ds_string` **−5.4%** | 100 万回の追記 268 → 152 ms | `a423a92` |
| 19 | 答えが文字列でない String のネイティブ 11 本が**借りる** | `size`・`==`・`<=>`・`bytesize` などが、長さや真偽を答えるために文字列を丸ごと複製していた | `ds_string` **−11.3%** | `+` −19.7%、`size` −11.3% | `f83cc81` |
| 20 | 命令ループの頭が「何か起きているか」を**1 回だけ聞く** | タスクのティック・切り替え・ステップ予算・GC の 4 つを毎命令 4 か所から読んで 4 回分岐していた | 8 本すべて **−1.1〜−4.6%** | `bm_fib` −4.6%、`loop_while_add` −4.2% | `e26ccff` |
| 21 | `OP_ADD` と 4 つの比較が**数どうしならその場で答える** | `op_arith` の中で「どの演算か」を symbol の比較で聞き直していた（ディスパッチが決めたことの二度手間） | 8 本すべて **−0.9〜−6.7%** | `bm_so_mandelbrot` −6.7%、`bm_fib` −4.2% | `a0ef97e` |

14 は速さのための変更ではなく、Ruby で書いた `[]` の中でタスクを止められるようにするための変更
（`docs/design/fibers.md` の Native boundaries、`docs/worklog/2026-09-15-getidx-dispatch.md`）で、
早道はそのついでに本家から写したものである。合計が ±0 で分類だけ動くのはそのため。
なお 14 の測定は、4 節の「コード配置の揺れ」が一番はっきり出た例でもある: 意味の同じ 3 つの書き方で
`call_kwargs` と `loop_while_add` の当たり外れが入れ替わり、片方を直すともう片方が 12% 遅くなった。

段階 0 で `unsafe` を消したときに 2.5% 遅くなり、段階 3 でクロージャを足したときに 1.8% 遅くなった。
4 と 3 はその 2 つを取り戻したもの（取り戻したうえで、出発点より速い）。

## 3. 各改善の解説

### 3.1 統計のカウンタが最大の無駄だった（#1）

**症状**: 命令ループの素の速さを測る `loop_while_add` などが本家の 4 倍。

**原因**: 実行ループに `self.op_counts[byte as usize] += 1;` の 1 行があった。Playground の opcode ヒストグラムと、
本家テストの「Opcodes executed: n / 119」のための統計で、`Vec<u64>` への加算。計画では「せいぜい 1%」と見ていたが、
この行を消しただけのビルドと比べると、`loop_while_add` で **25%**、`bm_so_mandelbrot` で **21%** 違った。
1 命令あたり 5〜7 ns の世界では、(1) `Vec` のデータポインタと長さの読み直し（`&mut self` が動き回るので毎回）、
(2) 範囲チェック、(3) 同じ番地（同じ命令が続けば同じ 8 バイト、違っても同じキャッシュライン）への
読んで足して書く、が毎命令つながり、(3) のストア→ロードの依存鎖だけで 1 ns 前後を占める。

**変更**: `[u64; 119]` の固定長配列を `Vm` の中に直に置き、添字を生のバイトではなく解読後の `op as usize` にした
（列挙が 119 variant なので範囲チェックが消える）。そのうえで実行時フラグ `count_ops` を足し、**既定はオフ**。
`Vm::set_op_counting(true)` で数える（Playground と `mrbtest` はオンにする）。

**捨てた案**: 「`Vec` のまま、フラグで数えない」——数えるのをやめたのに、固定長配列で数え続けるより遅かった
（ポインタと長さの読み出しが残り、そこに分岐が足される）。器を変えないと分岐だけでは勝てない。
feature で消す案は、CLI が既定で有効にするとベンチに戻り、無効にすると `mrbtest` の統計が出せなくなるので採らなかった。
フラグ版と「コードが無い」版の差は 2.6〜6.7% しかない。

**本家との関係**: 本家は同種の計数を `MRB_DEBUG` 系のビルド時設定に閉じている。統計はオプトイン、が正しい形。

### 3.2 毎命令のフレーム情報の複製（#2）

**症状**: 命令ループ・呼び出し・データ構造、すべてに薄く乗っていた。

**原因**: ループの先頭で `let ci = self.ci.last().unwrap().clone();` として `CallInfo`（13 フィールド、56 バイト）を
丸ごと複製していた。命令本体が使うのは `base`、`irep`、`pc` の 3 つ。複製そのものに加え、複製を持ち続けることで
レジスタが圧迫される。

**変更**: 3 フィールドだけを Copy で取り出す。5 候補の中で最も広く効いた（`bm_so_mandelbrot` −7.1%、`loop_times` −6.0%、
`ds_array` −5.7%、fib −4.5%）。

### 3.3 メソッド探索が値を複製して返していた（#3）

**症状**: 段階 3 で `Method` に `Arc<dyn Fn>` を持つ variant（クロージャ）を足したら、fib が 4% 遅くなった。

**原因**: `find_method` が `Method` を値で返し、呼び出し側が `#[derive(Clone)]` で複製する。variant がすべて Copy 相当の
間は 16 バイトの memcpy だったが、`Arc` が入ると「判別子で分岐して、片方の腕で atomic 増加」になる。
呼び出しごとにそれが走る。

**変更**: ディスパッチ経路には Copy な `MethodRef`（`Ruby(ObjId)` / `Native(fn)` / `Closure(u32)` など）を返す
`find_method_ref` を用意し、クロージャの実体は必要になったときだけ `closure_of` で引く。
fib −5.2%、`call_args` −4.7%、`app_tak` −6.3%。段階 3 で失った分がそのまま戻り、さらに速い。

### 3.4 `unsafe` を消した代償を取り戻す（#4）

**経緯**: 命令バイトを列挙 `Op` にする `from_u8` は `transmute` だった（VM の crate で唯一の `unsafe`）。
段階 0 で 119 要素の表引きに変えたら **2.5%**（ディスパッチ律速のループで 6〜7%）遅くなった。`transmute` は範囲チェックだけで
**読み出しが無い**（`u8` と `Op` は同じビット列）のに対し、表引きは毎命令 1 バイト読む。「コストは同じ」という見込みが外れた例。

**変更**: 119 本の `match`（`0 => Op::Nop, 1 => Op::Move, …`、生成器 `tools/gen_opcode.py` が出す）。
判別子と値が同じ列挙なので、コンパイラは `match` を範囲チェックだけに畳む。`objdump` で確かめた: 表引きのロードが消えている。
`unsafe` は 0 のまま、`loop_while_add` −12%、全体 −1.7%。

### 3.5 メソッドキャッシュ（#5）

**原因**: 探索はクラスチェーンを歩いて節ごとに `HashMap<Sym, Method>` を引く。インラインキャッシュは無かった。

**設計で怖いのは速度ではなく無効化**。メソッド表を書き換える入口は 40 か所以上ある（`def`、`undef_method`、`remove_method`、
`alias`、`define_method`、`include`、`prepend`、`extend`、`initialize_copy`、`$~` 系を `Undef` で塞ぐところ、…）。
1 つずつ「ここでキャッシュを捨てる」と書いて回ると漏れる。

**変更**: `ClassData` への可変アクセスは `Heap::class_mut` の 1 本しか通らないことを `grep` で確かめ、
**その 1 行で世代番号（`method_serial`）を上げる**。「クラスに可変アクセスした＝答えが変わったかもしれない」という粗い判定で、
定数代入でもキャッシュが飛ぶが、正しさは漏れない。GC が回収したクラスの `ObjId` が別のクラスに再利用される穴は、
`Heap::alloc` でクラスを作るときにも番号を上げて塞いだ。形は 1024 行の直接マップ（1 way）、行は
`(serial, class, mid, Option<(MethodRef, owner)>)`、見つからなかったことも覚える（`method_missing` の繰り返しのため）。
外れのコストは比較 3 回。

**確認**: 20 通りの無効化（定義・再定義・`remove`/`undef`・`include`/`prepend`・特異・`extend`・`define_method`・
`alias`・`private` 化・`class_eval`・`Comparable` の差し替え・…）を、**古い答えを 200 回呼んでキャッシュに載せてから**変更する
スクリプトで確かめ、期待値は本家 4.1.0-rc の出力（`tests/custom/method_cache`）。効果は −1.3% と小さく、
探索そのものより 3.1〜3.3 の方が重かった。

### 3.6 `String#[]` が文字列全体を複製していた（#6）

**症状**: `ds_string`（1500 バイト前後の文字列）の 7 割が `String#[]`。15 バイトなら 98 ms、1500 バイトなら 724 ms と、
**長さに比例**していた。

**原因**: O(n) が 2 つ重なっていた。(1) `bytes(vm, s)` が `to_vec()`、つまり呼ぶたびに文字列全体を複製する。
(2) `char_len` と `char_to_byte` が UTF-8 を 1 バイトずつ歩いて文字を数える。

**変更**: (1) 全体を複製せず、`str_bytes` を短く 3 回借りる形にした（間に挟まる `index_args` は `to_int` を呼ぶことも
例外を投げることもあるので `&mut Vm` が要り、借用を持ち越せない。だから 3 回）。割り当てるのは切り出した断片だけ。
(2) ASCII の早道: `b.is_ascii()`（機械語 1 語ずつ）が真なら文字数＝バイト数。多バイトの文字列は 1 回余分に走る。
1500 バイトの `[]` が 738 → 94 ms（**7.9 倍**）で、15 バイトと同じ速さになった。`utf8` 有りと無しの両方の本家テストが基準どおり。

### 3.7 Hash の走査（#7）と索引（#9、#10）

**症状**: `ds_hash`（1000 要素）で、同じ回数の出し入れが 10 要素なら 94 ns、1000 要素なら 660 ns。差を平均走査長 500 で割ると
**1 要素 1.2 ns**、要素数によらない固定費が **90 ns**（ネイティブ呼び出しと引数の受け渡し）。
`performance.md` の「挿入順の線形探索（本家の AR モードのみ）」の予想どおり。

**#7（定数を落とす）**: 走査のループが 1 要素ごとに `self.heap.get(o)` を取り直していた。鍵の一致確認 `key_eql` は Ruby の `eql?` を
呼ぶことがあるので `&mut self` が要り、借用を持ち越せないため。走査と確認を分け、ハッシュ値の `Vec<i64>` をスライスとして 1 回借りて
`position()` で候補を探し、**候補が見つかったときだけ**借用を抜けて `eql?` を呼ぶ。1 要素 1.2 → 0.78 ns。O(n) は残る。

**#9（入口を 1 本に）**: 索引を足す前に、`HashData` の `entries` と `hashes` を非公開にして 7 つのメソッドを通させた（54 行）。
性能は変えない（−0.3%）。前の担当が数えた書き込み箇所「37 か所」には 1 か所の数え漏れがあり、非公開にしたらコンパイラが指摘した。
3.5 の `class_mut` と同じ考え方で、無効化の抜けをコンパイラに見つけさせる。

**#10（索引）**: 16 要素（本家の AR/HT の切り替えと同じ閾値）を超えたら、`hash 値 -> entries の添字` の索引を持つ。
バケットごとに先頭挿入の 1 本のチェイン、削除で作り直し。挿入順は `entries` の `Vec` が保つ。
キーが `eql?` で一意なので候補の順序を気にする必要がなく、本家より単純に書けた。
`ds_hash` −19.8%、`vm_optimization_bench` −14.4%（中に 5 万要素の Hash 操作がある）。閾値以下しか使わない
`app_json_hash` は ±0。索引ありと索引なし（#9 のバイナリ）で境界（15/16/17/40 要素、閾値を下回る削除、`shift`、
同一 `hash` で非 `eql?` の 25 キー、`rehash`、`replace`/`dup`/`merge`/`clear`/`compact!`）の出力が一致し、本家とも一致
（`tests/custom/hash_index_boundary`）。

**捨てた案**: バケットごとの `Vec<u32>`（キーの数だけ確保する）、削除で索引を捨てるだけ（削除と探索が交互だと二度と作られない）、
tombstone（`entries` の読み手全員が穴を飛ばす必要がある）。

### 3.8 `Array#shift` を O(1) に（#8）

**症状**: 本家のベンチ `bm_so_lists` 3685 ms のうち **2870 ms（78%）**が `shift` 単独。`shift` を `pop` に変えるだけで 446 ms になる。

**原因**: `ObjKind::Array` が `Vec<Slot>` で、`shift` は `Vec::remove(0)`。10000 要素の配列を 10000 回 shift するたびに
約 5000 万要素の前詰め（16 バイト × 5000 万 = 800 MB）、それを 300 回。本家はバッファを共有して先頭ポインタをずらす O(1)。

**変更**: `ArrayData { buf: Vec<Slot>, start: usize }`（非公開フィールド、`Deref<[Slot]>`）。`shift`/`shift(n)`/`unshift`/`insert(0)` が
O(1) で、死んだ先頭が生きている部分より長くなったら（`start > len && start > 16`）前詰めする（償却定数）。
`&buf[start..]` で今までどおりスライスが取れるので、`VecDeque` 案より波及が小さく、`pack`・`Struct`・`Marshal` は無変更で通った。
本家の「共有」の意味までは再現していない（「選べる実装」）。`bm_so_lists` 3699 → 999 ms（−73%）。

**隠れていた O(n) が表に出た**: 最初の計測で `ds_array` が **+155%** になった。原因は `ary_aset` の `let len = items(vm, s).len();`
——配列を丸ごと複製して長さだけ見る。`Heap::array` が `&Vec` を返していた間は LLVM がその割り当てごと消していたが、
`&[Slot]` に変えたら消えなくなり、元からあった O(n) が表に出た。長さだけ要る 8 か所を `ary_len` に替えて解消。
「最適化されていたから遅くなかった」コードは、周りを変えると突然遅くなる。

### 3.10 Hash を 1 回引くたびに全鍵を複製していた（#11、#12）

**症状**: 段階 2c で索引を入れて探索を O(1) にしたのに、`ds_hash` は −20% にとどまり、本家比 7.7x が残った。

**切り分け**（要素数だけを変え、引く鍵は先頭の 10 個に固定）: 探索の距離は要素数によらないはずなのに、1 回あたりの時間が
10 要素 44 ns、100 要素 66 ns、1000 要素 367 ns と 1 要素 0.32 ns で伸びる。**要素数に比例する分が残っていれば、それは探索ではない**。

**原因**: `hash_sync`（鍵の `hash` が古くなっていないかを確かめて必要なら計算し直す）が、「古いか」を聞く**前に**全要素の鍵を `Vec` に複製していた。
借用を早く切るために先に集めておく、というよくある形で、必要な場合（`rehash` の後など）にだけ要るものを毎回作っていた。
`vm_optimization_bench` の中の 5 万要素の Hash は、1 回の引きにつき 5 万要素を複製していたことになる。

**変更**: 順番を入れ替えただけ。全体 −11.7%、`ds_hash` 628 → 233 ms（本家比 7.4x → **2.7x**）、`vmo_objects` −85%、
キーワード引数は Hash で渡るので `call_kwargs` も −15%。あわせて `hash_set` が `key_hash` を 2 回計算し、既存の鍵でも String 鍵を
毎回複製して凍結していたのを、1 回・挿入時だけにした（#12。ベンチでは動かないが `h[k] = v` の micro で効く）。

**教訓**: 「固定費 90 ns はネイティブ呼び出しと引数の受け渡し」と 3.7 節に書いたのは間違いで、呼び出しの実費は 28 ns（`Array#[]` で実測）。
残りは Hash 自身の仕事だった。また、計画にあった「Integer/Symbol/String の鍵を Rust で答える早道」は**すでに入っていた**
（本家の `mrb_hash_ht_hash_func` と同じ形）。切り分けを先にしたから、無い仕事に手を付けずに済んだ。

### 3.11 `include?` がループ内で配列を複製していた（#13）

`Array#include?`/`member?`/`count` が 1 要素を比べるたびに `items()`（配列全体の複製）を呼んでいて O(n²)。100 要素の `count` で −86%。
ベンチ 22 本はどれも呼ばないので合計は動かないが、実コードでは普通に使う。`items()` の複製は、3.8 の 8 か所を直した時点で
ベンチからは消えていた（切り分けで確認）。

### 3.9 効かなかった・やらなかったもの

* `Slot` を 8 バイトにする（NaN boxing 相当。`performance.md`）——出発点の計測で「値の出し入れ」は主因でないと分かったので、後回し。
* 鍵が Integer/Symbol/String のときの `hash`/`eql?` の早道——調べたらすでにあった（3.10）。
* `unsafe` による境界チェックの省略、goto threading の模倣——方針としてやらない。
* ネイティブが配列を `items()` で複製する箇所（中身を読むもの）——長さだけの 8 か所は直したが、残りは残っている（5 節）。

## 4. 計測の方法

この一日で分かったことの中で、本に書く価値が一番あるのはこれかもしれない。

* **機械**: i7-13700（P コア + E コア）の WSL2。`taskset` 無しだと E コアに落ちて 20% 遅い。`tools/bench.sh --core 2`（P コア固定）が前提。
* **逐次の best of 5 が当てにならない**。Linux VM の中は idle でも、Windows ホスト側の都合で実行時間が数分単位で 10〜40% 動く。
  「A を 5 回 → B を 5 回」だと 5 回とも同じ遅い窓に入り、best が外乱を弾かない。同じ変更が同じ日の 2 回の計測で **+28% と −12%** に出た。
* **交互 A/B が効く**。1 ラウンドごとに A と B を 1 回ずつ回すと、両方が同じ分の同じ機械に当たるので、ミリ秒は動いても比は動かない。
  `tools/bench_ab.sh`。段階ごとの交互 A/B の積（−20.4%）と通しの計測（−22.0%）が一致した。
* **自分の道具も外乱になる**。計測中に `grep -rn` や `cargo build` を回すと best と median が 17〜44% 離れる。best と median の差が
  0.5% 程度なら静か、1% を超えたら取り直す。荒れている日は min of 15。
* **並行作業のベンチは互いを汚す**。2 人の担当が同じ機械でベンチを回した日の数値は捨てた。ベンチを伴う作業は同時に 1 つ。
* **呼び出しを含まないベンチは候補ごとに ±4〜12% 動く**。9000 行の `exec_frames` のループ本体がキャッシュラインのどこに乗るかの揺れ
  （コード配置）で、1 命令 5〜7 ns で他に何もしていないぶん一番敏感に出る。分類の合計で見る。
* **計測する木を取り違えない。** `tools/bench.sh` は `SABIRUBY_BIN` が無ければその場で `cargo build --release` を走らせるので、
  作業ツリーに未コミットの変更があるとそれを測る（ビルドが通ってしまうので気づきにくい）。基準を取り直すときは、
  測りたいコミットのバイナリを先に作って名前を付け、`SABIRUBY_BIN` で名指しする。
* **ベンチが呼ばないコードは micro で測る**（`bench/micro`、`tools/ab_micro.sh`）。公開ベンチ 27 本は増やさない。
  1 操作のループと、同じ形の空のループを並べて、**同じラウンドの空ループの動き**を雑音の底として引く。
  同じ変更（#17）が、空ループ −0.0% の回では −1.5%、空ループ −2.0% の回では −2.9% に出た。底を引けばどちらも −1% 前後で、
  底を見ないと「2 倍効いた」と読めてしまう。
* **切り分けは操作ごとの小さなスクリプト**で（`perf` はこの環境に無い）。空ループを一緒に測ってループ分を引く。
  要素数を変えて測ると固定費と 1 要素あたりのコストが分かれる（3.7）。
* **「コード配置の揺れ」の正体の半分は `codegen-units` だった**（2026-09-16）。`Cargo.toml` に `[profile.release]` は無いので
  既定の `codegen-units = 16` で、crate は 16 個のコード生成単位に切り分けられる。**どの関数がどの単位に入るかはソースが
  1 行変わると変わりうる**ので、`exec_frames` のインライン化と配置がそのたびに動く。`String#<<` を in-place にしただけの
  変更が `bm_so_mandelbrot` を **+16%**、`loop_while_add` を +10% にし、**2 回の独立した A/B で再現した**（外乱ではない）。
  同じ 2 つの木を `CARGO_PROFILE_RELEASE_CODEGEN_UNITS=1` で作り直すと、同じ A/B が −2.5% と +0.3% になった。
  **候補ごとの A/B は `codegen-units=1` のバイナリで取り、通しのベンチは既定のビルドで取る**（過去の基準と比べるため）、
  というのが 2026-09-16 の作業の決めだった。`codegen-units=1` にしても配置の揺れが消えるわけではない
  （1 行消しただけの偽物のバイナリが `bm_so_mandelbrot` を +8.5% にした例がある）が、±16% が ±8% になる。
* **2026-09-17、`[profile.release] codegen-units = 1` を既定にした**（`96221fb`、著者判断）。**以後、この repo の
  性能の数値はすべて 1 単位のビルドである**。出荷するバイナリも、候補ごとの A/B も、通しの基準も同じビルドになったので、
  前日の「A/B は cgu=1、通しは既定のビルド」という使い分けと、そこから出ていた二重帳簿（同じ段階が −4.2% と −1.2%）は無くなった。
  規則は 3 行で書ける: **`cargo build --release` がそのまま計測するバイナリ**、
  **`96221fb` より前の数値は cgu=16 なので、またぐ比較にはその断りを付ける**（`../verification/bench.md`）、
  **1 単位でも配置の揺れは ±8% 残るので、読むのは分類の合計**。
  決め手は 3 つ測ってから出した。**速さ**: 同じ木の 2 つのビルドの交互 A/B（7 ラウンド、27 本、`--core 2`、
  `bench/results/ab-cgu1.tsv`）で全体 **−2.0%**、27 本中 22 本が速く、遅い 5 本は +0.0〜+2.5%
  （`bm_mandel_term` −13.7%、`bm_so_mandelbrot` −11.9%、`call_fiber` −10.5%）。
  **大きさ**: x86_64 のコマンドが 5,621,344 → 4,678,600 バイト（**−16.8%**）、thumbv7em の rlib の graph が
  −8.9〜−11.8%（`../verification/size.md`）。1 単位なら同じ総称関数の実体が 1 つで済む。
  **代償はビルド時間**: clean な target からの `cargo build --release -p sabiruby-cli` が **9.2 s → 18.1 s**
  （best of 3、24 スレッドの機械）。CPU 時間はむしろ 58 s → 39 s と減る（重複した生成が消えるため）が、
  crate 1 つの中は並列にできないので壁時計は倍になる。日常の `cargo build` と `cargo test` は `dev` プロファイルなので影響を受けない。
* **偽物のバイナリで天井を測ってから候補を書く。** 「ループの頭の 4 つの確認」を**全部消した**（動かなくなってよい）
  ビルドが `vmo_dispatch` −10.5%、`vmo_arith` −9.0% を示したので、そこに 1 つ番人を置く候補を書いた（#20 は天井の 1/3 を取った）。
  同じやり方で `self.instructions += 1;` を消したビルドは**速くならなかった**ので、そちらには手を付けていない。

## 5. 残っている重さ

2026-09-16 の第 3 弾の切り分け（`../worklog/2026-09-16-perf3.md`）で、**残りの重さの中心が
「1 回の呼び出しの固定費」と「1 命令の固定費」であることがはっきりした**。データ構造の実装の
計算量の問題は、2026-09-15 でほぼ片付いている。

| 何 | 本家比 / 実測 | 中身 |
|---|---|---|
| **呼び出し 1 回の固定費** | 44 ns（本家 13 ns） | 引数の数はほとんど効かない（0 個 44 ns、3 個 50 ns）。`Vm` のフィールドの保存と復元が 4 組（`direct_send`、`native_ret_reg`、`pending_kw`、`native_active`）、`orphan_block_of_native`、`native_sampling` の確認、`cur`/`loop_exit` の確認。**引数リストの `Vec` の割り当てではない**（`Args` 型で 2 通り試して、どちらも遅くなった） |
| 省略可能な引数・`*rest` | +18 ns / +79 ns | `op_enter` の「必須引数だけ」の早道（`vm.rs:4441`）を外れると `argv: Vec<Value>` を作る道に落ちる |
| **1 命令の固定費** | +1.5〜4.8 ns（本家比 2.7〜3.6x） | ループの頭の 4 つの読み出し（番人 1 つに入れて 1/3 取った。残りは 4 つを 1 バイトにまとめること）、**1 命令に間接分岐が 2 回**（`match op.operands()` と `match op`。本家は 1 回）、`self.stack` の境界検査 |
| `bm_so_lists` | 4.09x | 内訳は `push` 600 万・`empty?` 600 万・`shift` 300 万・`pop` 300 万、つまり上の「呼び出しの固定費」そのもの。`reverse!`（0.44x）と `to_a`（0.61x）は本家より**速い**、`==` と `dup` は合わせて全体の 1.3% |
| `String` の `[]` と短い文字列 | `[]` 95 ns（本家 13 ns） | 複製も走査も無い 15 バイトの文字列でもこれだけかかる。本家は 22 バイトまでを `RString` に**埋め込む**（`RSTR_EMBED`）ので、`t[j]` の答えに malloc が要らない。SabiRuby は `ObjKind::String(Vec<u8>)` なので 1 文字でもヒープの塊を 1 つ取る |
| `String` の ASCII の旗 | `[]` の 23%、`size` の 20%（1000 バイトのとき） | 本家の `MRB_STR_ASCII` 相当。天井は測った（15 バイトと 1000 バイトの差）。やるなら `Vec<u8>` を `StrData` のような非公開フィールドの型にして、旗を古くする経路をコンパイラに見つけさせる形で |
| Hash | 2.7x | 残りは Hash 固有の 16 ns とネイティブ呼び出しの 28 ns。`hash`/`eql?` を Ruby で定義した鍵は 286 ns で、これは本家も同じ |
| `items()` の複製 | — | 「答えが小さいのに全体を複製する」ものは 2026-09-16 に片付いた（#15〜#17）。残るのは**答えが配列全体**のもので、`Vm::ary_new` が `Vec<Value>` を取る以上どのみち 1 回は写る |
| 値の表現 | — | `Slot` 16 バイト。**8 バイト化は 2026-09-16 に見送った**: 3b の切り分けが「帯域ではなく回数」を指したため（見積もりは `../worklog/2026-09-16-perf3.md` の 3d 節）。WASM と MCU では「速さ」ではなく「載るかどうか」の問題として測り直す |
| ベンチの穴 | — | 27 本すべてが数値を出す。`mem_retained` は本家の世代別 GC の弱点を測っているので、本家比 0.5x は SabiRuby が速いのではなく本家が遅い |

## 6. 性能に関わる設計の約束

* **`unsafe` を増やさない**（今 0）。境界チェックの省略や `transmute` で稼がない。
* **統計はオプトイン**（`set_op_counting`、`set_trace`）。既定の実行ループには計数を置かない。
* **書き換えの入口を 1 本にしてから、キャッシュや索引を足す**（クラスの表は `class_mut`、Hash は `HashData` のメソッド）。
  無効化の抜けは人が探すのではなく、コンパイラに見つけさせる。
* **本家の「振る舞い」を再現し、「表現」は選び直す**（配列の共有バッファ、Hash の AR/HT、インラインキャッシュの形）。
  選び直した表現の計算量が本家と違えば、それがそのまま倍率になる（3.8）。
* **変更は 1 つずつ、交互 A/B で測ってから採る**。効かないものは取り込まず、数値だけ残す。
